# ✅ MeetingMind AI - Успешно запущен!

## 🔐 РЕГИСТРАЦИОННЫЕ ДАННЫЕ

```
📧 Email:    test@test.com
🔑 Пароль:   test123456
```

---

## 🌐 ДОСТУП К ПРИЛОЖЕНИЮ

| Сервис | URL |
|--------|-----|
| 📱 **Frontend** | http://localhost:3002 |
| 🔧 **Backend API** | http://localhost:8000 |
| 📚 **API Docs** | http://localhost:8000/docs |
| 🧪 **Тест входа** | http://localhost:3002/test-login.html |

---

## 🚀 КАК ВОЙТИ

### Способ 1: Через браузер (рекомендуется)

1. Откройте **http://localhost:3002**
2. Нажмите **"Sign In"**
3. Введите email: `test@test.com`
4. Введите пароль: `test123456`
5. Нажмите кнопку входа

### Способ 2: Через тестовую страницу

1. Откройте **http://localhost:3002/test-login.html**
2. Нажмите кнопку **"Login"**
3. Токены сохранятся автоматически

### Способ 3: Через терминал (curl)

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=test@test.com&password=test123456"
```

**Ответ:**
```json
{
  "access_token": "eyJhbGci...",
  "refresh_token": "eyJhbGci...",
  "token_type": "bearer"
}
```

---

## 📊 СТАТУС СЕРВИСОВ

| Сервис | Статус | Порт |
|--------|--------|------|
| Backend API | ✅ Работает | 8000 |
| Frontend | ✅ Работает | 3002 |
| База данных | ✅ SQLite | meetingmind.db |
| Тестовый пользователь | ✅ Создан | - |

---

## 🔧 Команды управления

### Проверить статус
```bash
# Backend
curl http://localhost:8000/health

# Frontend
curl http://localhost:3002
```

### Перезапустить Backend
```bash
cd /home/kostik/meetingmind/backend
pkill -f uvicorn
DATABASE_URL=sqlite:///./meetingmind.db ./venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### Перезапустить Frontend
```bash
cd /home/kostik/meetingmind/frontend
pkill -f "vite.*3002"
npm run dev -- --host 0.0.0.0 --port 3002
```

---

## 📁 Структура проекта

```
/home/kostik/meetingmind/
├── backend/
│   ├── app/              # Исходный код backend
│   ├── venv/             # Python virtual environment
│   ├── meetingmind.db    # SQLite база данных
│   └── create_user.py    # Скрипт создания пользователя
├── frontend/
│   ├── src/              # Исходный код frontend
│   └── test-login.html   # Тестовая страница входа
├── docs/                 # Документация
├── docker-compose.yml    # Docker конфигурация
└── .env                  # Переменные окружения
```

---

## ⚠️ Важно

- Frontend работает на порту **3002** (не 3000 или 3001)
- Backend работает на порту **8000**
- Для полноценной работы с PostgreSQL используйте `docker-compose up -d`

---

**Проект полностью работает! 🎉**
